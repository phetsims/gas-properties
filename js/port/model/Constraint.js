// Copyright 2019, University of Colorado Boulder

/**
 * A constraint that is applied to entities in the system.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
define( require => {
  'use strict';

  // modules
  const gasProperties = require( 'GAS_PROPERTIES/gasProperties' );

  class Constraint  {

    constructor() {
      //TODO
    }
  }

  return gasProperties.register( 'Constraint', Constraint );
} );